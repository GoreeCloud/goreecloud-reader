# GoreeCloud Reader Web Foundation

This is a dependency-free web application shell for Reader development.

It uses synthetic items only and intentionally contains no application network requests, persistence, authentication, file import, media parsing, reader engine, or audiobook playback.

Open `index.html` directly for the current preview. Search, filtering, selection, and unavailable-state feedback are functional.
